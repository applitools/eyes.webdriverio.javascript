'use strict';

class DomCapture {

  /**
   * @param {EyesWebDriver} driver
   * @return {Promise.<string>}
   */
  static getWindowDom(driver) {
    //todo need to implement
    return Promise.resolve('{}');
  }


  static _getFrameDom(driver, argsObj) {

  }
}

module.exports = DomCapture;
